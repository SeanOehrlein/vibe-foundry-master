# AI Assistant Skills

This directory contains atomic capabilities for the AI Assistant.

## Standard
*   Each skill must be in its own subdirectory.
*   Each skill must have a `manifest.json` defining inputs/outputs.
*   Skills should be stateless and modular.
